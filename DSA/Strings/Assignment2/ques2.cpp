// Find the second largest digit in the string consisting of digits from ‘0’ to ‘9’
#include <iostream>
#include <algorithm>
#include <string>
#include <cmath>
#include <climits>
using namespace std;
int main(){
    string s;
    getline(cin,s);
    int max=INT_MIN;
    int smax=0;
    for(int i=0; s[i]!='\0';i++){
        if(smax<=((int)s[i]-48)&& smax<)
    }
    
}